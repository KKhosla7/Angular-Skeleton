# Welcome to Angular Skeleton

This is Angular Skeleton which uses Express as a web server.

# Install

Download this project run it inside your IDE.

Install Express with `npm`:

```
npm install
```

Install dependencies with `bower`:

```
bower install
```